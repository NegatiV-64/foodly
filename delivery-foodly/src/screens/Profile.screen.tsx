import type { FC } from 'react';
import { Text, View } from 'react-native';

export const ProfileScreen: FC = () => {
    return (
        <View>
            <Text>
                Profile
            </Text>
        </View>
    );
};