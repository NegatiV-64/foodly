import type { FC } from 'react';
import { View, Text } from 'react-native';

export const LoginScreen:FC = () => {
    return (
        <View>
            <Text>
                Login
            </Text>
        </View>
    );
};